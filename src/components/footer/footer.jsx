import React from 'react';
import s from '../../styles/footer.module.css'

const Footer = () => {
    return (
        <footer className={s.footer}>это футер</footer>
    )
};

export default Footer;
